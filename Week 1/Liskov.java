A child class must act like its parent class. It shouldn’t break any rules or expectations set by the parent. 
If a child class can’t properly replace the parent, the code becomes unpredictable, buggy, and hard to maintain.

